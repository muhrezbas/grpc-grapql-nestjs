export enum SCMTransactionSupplierStatusType {
  AWAITING_PAYMENT = 'awaiting_payment',
  PENDING = 'pending',
  CONFIRM = 'confirm',
  PROCESSED = 'processed',
  ON_DELIVERY = 'on_delivery',
  SUCCESS = 'success',
  FAILED = 'failed',
  CANCELED = 'canceled',
}
