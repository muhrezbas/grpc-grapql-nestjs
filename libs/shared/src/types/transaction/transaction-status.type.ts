export enum TransactionStatus {
  AWAITING_PAYMENT = 'awaiting_payment',
  PENDING = 'pending',
  PROCESSED = 'processed',
  CANCELED = 'canceled',
  READY = 'ready',
  ON_DELIVERY = 'on_delivery',
  SUCCESS = 'success',
  FAILED = 'failed',
}
